<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
        <!--&lt;!&ndash; Customize your theme here. &ndash;&gt;-->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    <!--</style>-->

    <!-- 权限 -->
    <style name="permission_PermissionActivity" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:statusBarColor" tools:targetApi="lollipop">@android:color/transparent
        </item>
    </style>

    <!-- 气泡控件 -->
    <style name="bubble_dialog" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <!-- 毛玻璃 -->
    <declare-styleable name="BlurLayout">
        <attr name="blk_downscaleFactor" format="float" />
        <attr name="blk_blurRadius" format="integer" />
        <attr name="blk_fps" format="integer" />
        <attr name="blk_cornerRadius" format="dimension" />
    </declare-styleable>


    <!-- 按钮控件 -->
    <style name="btn_white_stroke_style">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/btn_height_46dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/bg_btn_white_stroke_shape</item>
    </style>

    <!-- 蓝色圆角按钮 -->
    <style name="btn_blue_solid_style" parent="btn_white_stroke_style">
        <item name="android:background">@drawable/bg_btn_blue_solid_shape</item>
    </style>

    <!-- 蓝色圆角按钮 -->
    <style name="btn_blue_stroke_style" parent="btn_white_stroke_style">
        <item name="android:textColor">@color/circle_btn_blue_bg</item>
        <item name="android:background">@drawable/bg_btn_blue_stroke_white_solid_shape</item>
    </style>

    <!-- 圆角EditText -->
    <declare-styleable name="CircularEditText">
        <attr name="hint" format="string"/>
        <attr name="text_size" format="dimension"/>
        <attr name="text_color" format="color"/>
        <attr name="line_default_color" format="color"/>
    </declare-styleable>

</resources>
